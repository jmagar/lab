Create embeddings | OpenAI API Reference
[Skip to content](#_top)
[API Reference](/api/reference/java)
[Embeddings](/api/reference/java/resources/embeddings)
Copy Markdown
Open in **Claude**
Open in **ChatGPT**
Open in **Cursor**
**Copy Markdown**
**View as Markdown**
# Create embeddings
[CreateEmbeddingResponse](</api/reference/java/resources/embeddings#(resource) embeddings > (model) create_embedding_response > (schema)>) embeddings().create(EmbeddingCreateParamsparams, RequestOptionsrequestOptions = RequestOptions.none())
POST/embeddings
Creates an embedding vector representing the input text.
##### ParametersExpand Collapse
EmbeddingCreateParams params
Input input
Input text to embed, encoded as a string or array of tokens. To embed multiple inputs in a single request, pass an array of strings or array of token arrays. The input must not exceed the max input tokens for the model (8192 tokens for all embedding models), cannot be an empty string, and any array must be 2048 dimensions or less. [Example Python code](https://cookbook.openai.com/examples/how_to_count_tokens_with_tiktoken) for counting tokens. In addition to the per-input token limit, all embedding models enforce a maximum of 300,000 tokens summed across all inputs in a single request.
String
[](<#(resource) embeddings > (method) create > (params) default > (param) body > (schema) > (property) input > (variant) 0>)
List\<String\>
[](<#(resource) embeddings > (method) create > (params) default > (param) body > (schema) > (property) input > (variant) 1>)
List\<long\>
[](<#(resource) embeddings > (method) create > (params) default > (param) body > (schema) > (property) input > (variant) 2>)
List\<List\<long\>\>
[](<#(resource) embeddings > (method) create > (params) default > (param) body > (schema) > (property) input > (variant) 3>)
[](<#(resource) embeddings > (method) create > (params) default > (param) body > (schema) > (property) input>)
[EmbeddingModel](</api/reference/java/resources/embeddings#(resource) embeddings > (model) embedding_model > (schema)>) model
ID of the model to use. You can use the [List models](https://platform.openai.com/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](https://platform.openai.com/docs/models) for descriptions of them.
TEXT\_EMBEDDING\_ADA\_002("text-embedding-ada-002")
[](<#(resource) embeddings > (model) embedding_model > (schema) > (member) 0>)
TEXT\_EMBEDDING\_3\_SMALL("text-embedding-3-small")
[](<#(resource) embeddings > (model) embedding_model > (schema) > (member) 1>)
TEXT\_EMBEDDING\_3\_LARGE("text-embedding-3-large")
[](<#(resource) embeddings > (model) embedding_model > (schema) > (member) 2>)
[](<#(resource) embeddings > (method) create > (params) default > (param) body > (schema) > (property) model>)
Optional\<Long\> dimensions
The number of dimensions the resulting output embeddings should have. Only supported in `text-embedding-3` and later models.
minimum1
[](<#(resource) embeddings > (method) create > (params) default > (param) body > (schema) > (property) dimensions>)
Optional\<EncodingFormat\> encodingFormat
The format to return the embeddings in. Can be either `float` or [`base64`](https://pypi.org/project/pybase64/).
FLOAT("float")
[](<#(resource) embeddings > (method) create > (params) default > (param) body > (schema) > (property) encoding_format > (member) 0>)
BASE64("base64")
[](<#(resource) embeddings > (method) create > (params) default > (param) body > (schema) > (property) encoding_format > (member) 1>)
[](<#(resource) embeddings > (method) create > (params) default > (param) body > (schema) > (property) encoding_format>)
Optional\<String\> user
A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](https://platform.openai.com/docs/guides/safety-best-practices#end-user-ids).
[](<#(resource) embeddings > (method) create > (params) default > (param) body > (schema) > (property) user>)
[](<#(resource) embeddings > (method) create > (params) default>)
##### ReturnsExpand Collapse
class CreateEmbeddingResponse:
List\<[Embedding](</api/reference/java/resources/embeddings#(resource) embeddings > (model) embedding > (schema)>)\> data
The list of embeddings generated by the model.
List\<double\> embedding
The embedding vector, which is a list of floats. The length of vector depends on the model as listed in the [embedding guide](https://platform.openai.com/docs/guides/embeddings).
[](<#(resource) embeddings > (model) embedding > (schema) > (property) embedding>)
long index
The index of the embedding in the list of embeddings.
[](<#(resource) embeddings > (model) embedding > (schema) > (property) index>)
JsonValue; object\_ "embedding"constant"embedding"constant
The object type, which is always “embedding”.
[](<#(resource) embeddings > (model) embedding > (schema) > (property) object>)
[](<#(resource) embeddings > (model) create_embedding_response > (schema) > (property) data>)
String model
The name of the model used to generate the embedding.
[](<#(resource) embeddings > (model) create_embedding_response > (schema) > (property) model>)
JsonValue; object\_ "list"constant"list"constant
The object type, which is always “list”.
[](<#(resource) embeddings > (model) create_embedding_response > (schema) > (property) object>)
Usage usage
The usage information for the request.
long promptTokens
The number of tokens used by the prompt.
[](<#(resource) embeddings > (model) create_embedding_response > (schema) > (property) usage > (property) prompt_tokens>)
long totalTokens
The total number of tokens used by the request.
[](<#(resource) embeddings > (model) create_embedding_response > (schema) > (property) usage > (property) total_tokens>)
[](<#(resource) embeddings > (model) create_embedding_response > (schema) > (property) usage>)
[](<#(resource) embeddings > (model) create_embedding_response > (schema)>)
### Create embeddings
Java
HTTP
HTTP
TypeScript
TypeScript
Python
Python
Java
Java
Go
Go
Ruby
Ruby
Terraform
Terraform
```
`package com.openai.example;
import com.openai.client.OpenAIClient;
import com.openai.client.okhttp.OpenAIOkHttpClient;
import com.openai.models.embeddings.CreateEmbeddingResponse;
import com.openai.models.embeddings.EmbeddingCreateParams;
import com.openai.models.embeddings.EmbeddingModel;
public final class Main {
private Main() {}
public static void main(String[] args) {
OpenAIClient client = OpenAIOkHttpClient.fromEnv();
EmbeddingCreateParams params = EmbeddingCreateParams.builder()
.input("The quick brown fox jumped over the lazy dog")
.model(EmbeddingModel.TEXT\_EMBEDDING\_3\_SMALL)
.build();
CreateEmbeddingResponse createEmbeddingResponse = client.embeddings().create(params);
}
}`
```
```
`{
"object": "list",
"data": [
{
"object": "embedding",
"embedding": [
0.0023064255,
-0.009327292,
.... (1536 floats total for ada-002)
-0.0028842222,
],
"index": 0
}
],
"model": "text-embedding-ada-002",
"usage": {
"prompt\_tokens": 8,
"total\_tokens": 8
}
}
`
```
##### Returns Examples
```
`{
"object": "list",
"data": [
{
"object": "embedding",
"embedding": [
0.0023064255,
-0.009327292,
.... (1536 floats total for ada-002)
-0.0028842222,
],
"index": 0
}
],
"model": "text-embedding-ada-002",
"usage": {
"prompt\_tokens": 8,
"total\_tokens": 8
}
}
`
```