Embeddings | OpenAI API Reference
[Skip to content](#_top)
[API Reference](/api/reference/terraform)
Copy Markdown
Open in **Claude**
Open in **ChatGPT**
Open in **Cursor**
**Copy Markdown**
**View as Markdown**
# Embeddings
Get a vector representation of a given input that can be easily consumed by machine learning models and algorithms.
#### resource openai\_embedding
##### required Expand Collapse
input: String
Input text to embed, encoded as a string or array of tokens. To embed multiple inputs in a single request, pass an array of strings or array of token arrays. The input must not exceed the max input tokens for the model (8192 tokens for all embedding models), cannot be an empty string, and any array must be 2048 dimensions or less. [Example Python code](https://cookbook.openai.com/examples/how_to_count_tokens_with_tiktoken) for counting tokens. In addition to the per-input token limit, all embedding models enforce a maximum of 300,000 tokens summed across all inputs in a single request.
[](<#(resource) embeddings > (terraform resource) > (attribute) input>)
model: String
ID of the model to use. You can use the [List models](https://platform.openai.com/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](https://platform.openai.com/docs/models) for descriptions of them.
[](<#(resource) embeddings > (terraform resource) > (attribute) model>)
##### optional Expand Collapse
dimensions?: Int64
The number of dimensions the resulting output embeddings should have. Only supported in `text-embedding-3` and later models.
[](<#(resource) embeddings > (terraform resource) > (attribute) dimensions>)
user?: String
A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](https://platform.openai.com/docs/guides/safety-best-practices#end-user-ids).
[](<#(resource) embeddings > (terraform resource) > (attribute) user>)
encoding\_format?: String
The format to return the embeddings in. Can be either `float` or [`base64`](https://pypi.org/project/pybase64/).
[](<#(resource) embeddings > (terraform resource) > (attribute) encoding_format>)
##### computed Expand Collapse
object: String
The object type, which is always “list”.
[](<#(resource) embeddings > (terraform resource) > (attribute) object>)
data: List[Attributes]
The list of embeddings generated by the model.
embedding: List[Float64]
The embedding vector, which is a list of floats. The length of vector depends on the model as listed in the [embedding guide](https://platform.openai.com/docs/guides/embeddings).
[](<#(resource) embeddings > (terraform resource) > (attribute) data > (attribute) embedding>)
index: Int64
The index of the embedding in the list of embeddings.
[](<#(resource) embeddings > (terraform resource) > (attribute) data > (attribute) index>)
object: String
The object type, which is always “embedding”.
[](<#(resource) embeddings > (terraform resource) > (attribute) data > (attribute) object>)
[](<#(resource) embeddings > (terraform resource) > (attribute) data>)
usage: Attributes
The usage information for the request.
prompt\_tokens: Int64
The number of tokens used by the prompt.
[](<#(resource) embeddings > (terraform resource) > (attribute) usage > (attribute) prompt_tokens>)
total\_tokens: Int64
The total number of tokens used by the request.
[](<#(resource) embeddings > (terraform resource) > (attribute) usage > (attribute) total_tokens>)
[](<#(resource) embeddings > (terraform resource) > (attribute) usage>)
### openai\_embedding
Terraform
HTTP
HTTP
TypeScript
TypeScript
Python
Python
Java
Java
Go
Go
Ruby
Ruby
Terraform
Terraform
```
`resource "openai\_embedding" "example\_embedding" {
input = "The quick brown fox jumped over the lazy dog"
model = "text-embedding-3-small"
dimensions = 1
encoding\_format = "float"
user = "user-1234"
}
`
```